#include <stdio.h>

/*
LISTA DELLA SPESA (o esercizi svolti)
9.1. Si scriva un programma che legga due valori interi, calcoli la
loro somma e media aritmetica, e le stampi a schermo.

9.2. Dato il seguente frammento di codice, disegnarne la memoria
e stabilire quale valore assume max.

9.3. Si scriva un programma che legga un valore intero e visualizzi
il valore intero precedente e il successivo.

9.4. Si scriva un programma capace di compiere le 4 operazioni
(somma, sottrazione, moltiplicazione e divisione) tra due numeri reali inseriti da tastiera.
Dopo che sono stati inseriti i due numeri, detti a e b, il programma
dovrà visualizzare i quattro valori a + b, a − b, ab, a/b. Si ipotizzi che b̸ = 0

9.5. Si realizzi un programma che acquisisca da tastiera un nu-
mero x e stampi un messaggio che indichi se tale numero sia positivo oppure
negativo, e ne stampi il valore assoluto |x|. Si richiede di risolvere l’esercizio
senza alcuna variabile di appoggio.

9.6. Si scriva un programma per calcolare
la media aritmetica di una serie di numeri x1, . . ., xn inseriti da tastiera,
ovvero la sommatoria da i=1 a k di x_i divisa per n.

9.7. (Iterazione indefinita con min-max). Si scriva un programma
per calcolare il valore massimo e minimo di un insieme di n numeri inseriti
da tastiera. Il programma deve prima leggere il valore di n dall’utente, ed in
seguito leggere una sequenza di n > 0 numeri. A questo punto il programma
deve stampare il massimo ed il minimo tra i numeri inseriti

9.8. Dati due interi positivi n e k da tastiera, calcolare la som-
matoria
S(n,k) = sommatoria da i=1 a n di k^i; k^1 + k^2 + ... + k^n

9.9. Sequenza di Fibonacci

9.10. Coefficiente binomiale nCr
*/

// Esercizio 9.1.
float somma(float x, float y)
{
	return (x+y);
}




int main(void)
{

}
